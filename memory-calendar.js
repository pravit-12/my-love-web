(() => {
  const select = document.getElementById("memory-select");
  const display = document.getElementById("memory-display");

  if (!select || !display) return;

  const memories = {
    first: "วันนั้นอาจจะเป็นแค่ข้อความแรกที่เราทักกัน แต่สำหรับเค้า มันคือจุดเริ่มต้นของความสบายใจแบบใหม่ในชีวิตเลย 💕",
    meet: "ตอนเจอกันครั้งแรก เค้าทั้งเขิน ทั้งเกร็ง ทั้งดีใจ แค่ได้เห็นเธออยู่ตรงหน้า ใจมันก็เต้นแรงไปหมดเลยนะ 🥺",
    official: "วันที่เราเป็นของกันและกันแบบชัดเจน เค้าจำได้เลยว่าคิดในใจว่า ‘ต่อจากนี้ไปอยากดูแลคนนี้ให้อยู่ในชีวิตนาน ๆ เลย’ 💘"
  };

  select.addEventListener("change", () => {
    const value = select.value;
    if (!value || !memories[value]) {
      display.textContent = "";
      return;
    }
    display.textContent = memories[value];
  });
})();
